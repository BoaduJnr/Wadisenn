node_modules/
client/dist/
*.local

# Old Deno KV database and its export, both of which hold real financial data
data/
kv-export.json

# Local secrets (GEMINI_API_KEY, etc.) — never commit these
.env
.env.*
!.env.example
